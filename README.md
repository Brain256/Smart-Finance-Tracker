# Smart-Finance-Tracker

A pipeline that intercepts real-time bank notifications from an Android device, parses the data into financial categories using AI inference, and securely logs them into a private dashboard database.
